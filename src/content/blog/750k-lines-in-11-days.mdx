---
title: '750,000 Lines in 11 Days: The Bun Migration That Broke My Brain'
description: 'Someone ported Bun from Zig to Rust: 750K lines, 99.8% of tests passing, 11 days, using Claude Opus 4.8''s dynamic workflows. Here''s how, and why it actually matters.'
pubDate: '2026-06-04'
heroImage: '/images/blog/bun-migration.webp'
tags: ['ai', 'agents', 'claude', 'workflow']
---

import StatStrip from '../../components/StatStrip.astro';

Every so often a single result moves the line of what feels possible. This one did it for me.

Jarred Sumner ported the Bun runtime from Zig to Rust. Roughly **750,000 lines of Rust, 99.8% of the existing test suite passing, eleven days from first commit to merge.** Not a toy, not a demo. The actual runtime, ported and green.

<StatStrip stats={[
	{ value: 750000, display: '750K', label: 'lines of Rust, ported' },
	{ value: 99.8, suffix: '%', label: 'of the test suite passing' },
	{ value: 11, label: 'days, first commit to merge' },
]} />

It was done with Claude Opus 4.8's dynamic workflows, and the *how* is the interesting part.

## How you migrate 750K lines without losing your mind

A migration that size usually fails for one reason: it's too big to hold in one head, or one context window. The trick was to stop treating it as one job.

**First, a workflow mapped the hard part.** Rust's ownership model is what makes a Zig-to-Rust port painful, so one workflow went through the codebase and worked out the right Rust lifetime for every struct field. The mechanical-but-brutal part, done systematically.

**Then, hundreds of agents ported in parallel.** The next workflow wrote every `.rs` file as a behavior-identical port of its `.zig` counterpart. Not sequentially: hundreds of agents at once, each on its own file, with two reviewers checking every file.

**Finally, a fix loop drove it to green.** A loop ran the build and the test suite, read the failures, and patched until both ran clean.

The shape is the lesson: decompose the impossible task, fan it out across parallel agents, verify every piece, then loop the whole thing to done.

## What "dynamic workflows" changed

The reason this wasn't possible a year ago comes down to what Opus 4.8 added. Dynamic workflows let the model plan a task, generate the orchestration to run it, fan out across hundreds of subagents, verify outputs before surfacing them, and resume interrupted jobs without starting over. That last bit matters more than it sounds: a job this long *will* get interrupted, and restarting from zero would kill it.

The verification is the quiet hero. Hundreds of agents producing code is worthless if you can't trust any of it. Two reviewers per file plus a test-driven fix loop is what turns "a lot of generated code" into "a migration." Volume without verification is just a bigger mess, faster.

## Don't take the wrong lesson

A result this loud invites a bad takeaway, so a few things to keep straight.

**It worked because the target was verifiable.** A port has a near-perfect oracle: the original behavior, encoded in an existing test suite. "Pass these tests" and "match this `.zig` file" are checkable a thousand times in parallel. Most work isn't that crisp. The technique shines where you have a hard, mechanical objective and a strong way to grade it, and gets shakier the fuzzier the goal.

**99.8% is not 100%.** That last fraction of a percent on a runtime is real software that behaves differently, and finding it is human work. The agents got a green-enough port astonishingly fast; they didn't absolve anyone of owning the gap.

**Years of human-written tests did half the work.** The migration leaned on Bun's existing suite as its safety net. The lesson isn't "agents replace engineers." It's that engineers who already invested in verification can now move at a speed that looks unreasonable.

So the takeaway isn't "AI does migrations now." It's that the ceiling on what one engineer can attempt jumped by an order of magnitude, on one condition: you can define *done* precisely enough to verify it at scale. Where you can, the results look like this. Where you can't, no amount of parallel agents will save you.
